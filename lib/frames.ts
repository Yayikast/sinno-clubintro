import { theme, getFrameById } from "@/themes";

export const FRAMES = theme.frames.list;
export const DEFAULT_FRAME_ID = theme.frames.defaults.frameId;
export const DEFAULT_FRAME_COLOR = theme.frames.defaults.frameColor;
export const DEFAULT_TEXT_COLOR = theme.frames.defaults.textColor;
export const DEFAULT_CAPTION = theme.frames.defaults.caption;
export const FRAME_COLOR_SWATCHES = theme.frames.frameColorSwatches;
export const TEXT_COLOR_SWATCHES = theme.frames.textColorSwatches;
export { getFrameById };
